{
  "installCommand": "npm install --include=dev",
  "buildCommand": "prisma generate && vite build",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api"
    },
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}